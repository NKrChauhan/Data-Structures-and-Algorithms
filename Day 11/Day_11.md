# Data Structure and Algorithm

## Day 11

#### Mostly it has functions for Searching and related Problems

### The functions applied in day 11 :
```sh
    int linearSearch(int a[],int n, int x);

    int orderedLinearSearch(int a[], int n, int x);

    int binarySearchIterative(int a[], int n, int x);

    int binarySearchRecursive(int a[], int n, int x, int s, int e);

    int interpolationSearch(int a[], int n, int x);

    bool checkDuplicateBruteForce(int a[],int n);

    bool checkDuplicateSorted(int a[],int n);

    bool checkDuplicateHash(int a[], int n);

    bool checkDuplicateNegate(int a[],int n);   

    int maxRepetitionBrute(int a[], int n);    

    int maxRepetitionRange(int a[], int n);

    int firstRepetitionHash(int a[], int n);    

```

## Output:
```sh
    The linear search gives the output of: 2
    The ordered linear search gives the output of: 2
    The binary search iterative gives the output of: 5
    The binary search recurssive gives the output of: 5
    The interpolation search gives the output of: 5
    The check duplication bruteforce method gives the output of: 1
    The check duplication in sorted method gives the output of: 1
    The check duplication in hashmap method gives the output of: 1
    The check duplication negation method gives the output of: 1
    The max repetition bruteforce present in array gives the output of: 4
    The max repetition range present in array gives the output of: 9
    The first repetition element present in array gives the output of: 8
```
